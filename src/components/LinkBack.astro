---
const linkbackPath = `/${Astro.url.pathname.split("/")[1]}`;

const { path } = Astro.props as Props;
interface Props {
  path?: string
}
---

<div class="text-center mt-12 mb-16">
  <a href={path || linkbackPath} class="brutalist-button--light inline-block">
    Back to all Posts</a
  >
</div>
